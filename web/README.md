To start the application, please enter the following commands:

cd web
npm install
npm run start

The application will be launched at the localhost:devServer address (devServer == port).
To change port go to webpack.config.js file.